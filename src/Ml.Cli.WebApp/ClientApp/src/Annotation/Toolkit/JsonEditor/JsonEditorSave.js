import Button from "@axa-fr/react-toolkit-button";
import React from "react";

const JsonEditorSave = ({onSubmit}) => <Button onClick={() => onSubmit()}>Save</Button>;

export default JsonEditorSave;
