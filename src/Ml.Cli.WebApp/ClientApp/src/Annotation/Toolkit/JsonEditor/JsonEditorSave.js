import Button from "@axa-fr/react-toolkit-button";
import React from "react";

const JsonEditorSave = ({onSubmit}) => <Button onClick={() => onSubmit()}>Sauvegarder</Button>;

export default JsonEditorSave;
